# Knowledge base (RAG) files

- `devops_engineer.md` — five ` ```json ` … ` ``` ` chunks, `role` = **DevOps Engineer**
- `ai_engineer.md` — five chunks, `role` = **AI Engineer**

Edit the fenced JSON blocks, then re-run the ingest script (see `docs/rag-setup.md`).
